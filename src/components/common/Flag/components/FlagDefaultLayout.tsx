import React from 'react';

const classes = {
  wrapper: 'rounded h-[1.625rem] w-[2.125rem] object-cover shadow bg-white',
};

const FlagDefaultLayout = () => {
  return <div className={classes.wrapper} />;
};

export default FlagDefaultLayout;
